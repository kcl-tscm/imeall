The models for the SQL and directory database. Along with classes
for performing maintenance of the grain boundary database.

Maintenance and Queries
=======================
These classes perform maintenance and query operations on the
directory tree structure.

.. automodule:: imeall.models
  :members:

SQL Models
==========
These classes define the data models for the canonical grains
and SubGrain models.

.. automodule:: imeall.gb_models
  :members:

